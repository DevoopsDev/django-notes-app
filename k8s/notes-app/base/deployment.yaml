apiVersion: apps/v1
kind: Deployment
metadata:
  name: notes-app
spec:
  selector:
    matchLabels:
      app: notes-app
  template:
    metadata:
      labels:
        app: notes-app
    spec:
      containers:
      - name: notes
        image: nginx:1.25   # (we’ll replace with your image next)
        ports:
        - containerPort: 80
  envFrom:
    - secretRef:
        name: django-secrets
